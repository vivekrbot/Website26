SANITY_PROJECT_ID=your-project-id
SANITY_DATASET=production

# Only needed once, to run `npm run seed:sanity` (migrates existing content into Sanity).
# Create a token with write access at https://www.sanity.io/manage -> API -> Tokens.
SANITY_API_TOKEN=
